<?php

/**
 * Implements hook_form_alter().
 */
function nomads_listing_form_alter(array &$form, \Drupal\Core\Form\FormStateInterface $form_state, $form_id): void {
  \Drupal::logger('nomads_listing')->notice('form_alter fired for @id', ['@id' => $form_id]);
}

/**
 * Implements hook_entity_type_alter().
 *
 * Temporary no-op stub to prevent fatal error if Drupal expects this hook.
 */
function nomads_listing_entity_type_alter(array &$entity_types): void {
  // Intentionally empty.
}
