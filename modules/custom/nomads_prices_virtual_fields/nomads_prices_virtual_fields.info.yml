name: 'Nomads Prices Virtual Fields'
type: module
description: 'Compatibility wrapper. Price range virtual field provisioning moved to Nomads listing virtual fields.'
package: 'Nomads Display'
core_version_requirement: ^11

dependencies:
  - nomads_listing_virtual_fields:nomads_listing_virtual_fields
  - commerce_price:commerce_price
